const arr = [1, 2]
// arr.unshift(0) //[0,1,2]
// arr.push(3) // [1,2,3]
// 1位置是起始下标，0位置的数字代表删除几个元素，3位置代表从起始下标插入
// arr.splice(1, 0, 3) // [1,3,2]




arr = [1, 2, 3]
app.pop() // [1,2]
app.shift() //[2,3]



console.log(arr);
