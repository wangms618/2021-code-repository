// var climbStairs = function(n) {
//   var a = [1,2]
//   for(let i=2;i<n;i++){
//       a.push(a[i-1]+a[i-2])
//   }
//   return a[n-1]
// };

// 如果创建函数或创建局部变量时，在栈上面申请空间，在内存的高地址分配
var climbStairs = function(n) {
    
};